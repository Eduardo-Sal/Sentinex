#Python file for the camera on mobile robot, this will set up the camera
#generate url, start stream, stop stream, adjust camera settings (if possible),
#handle stream errors, restart camera, save snapshot if intruder detected, 
#record clip if possible, zoom, adjust camera angle, etc.

class CameraController:
    def __init__(self):
        #Camera status and state
        #Camera settings
        #Camera stream settings
        #Camera error count
        #Camera recording settings
        #Camera position includes tilt, zoom, and movement 

    def setup_camera()

    def generate_stream_url()

    def start_video_stream()

    def stop_video_stream()

    def check_camera_status()

    def adjust_camera_settings()

    def camera_error_handler()

    def restart_camera()

    def save_screenshot()

    def save_clip()

    def zoom_camera()

    def move_camera()



