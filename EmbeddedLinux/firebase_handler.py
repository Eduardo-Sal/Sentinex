#Python file that handles firebase connection which assigns self to the device ID
#registers device, establish connection (API or websocket), push camera url to device 
#update device status (stopped, running, etc), send alert (notifications for anamolies)
#store device log, create json file if device is not registered

class DeviceManager:
    def __init__(self):
    def find_device()

    def register_new_device()

    def establish_connection()

    def push_camera_url()

    def update_device_status()

    def send_alert()

    def receive_commands()

    def handle_disconnect()

    def store_device_logs()

    def createJSON()